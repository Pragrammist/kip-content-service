namespace Core;

public enum FilmReleaseType
{
    RELEASE = default,
    PREMIERA = 1,
    SCREENING = 2
}

public enum GetOrderType
{
    ASC = default,
    DESC = 1
}

public enum FilmSortType
{
    ID = 0,
    
}